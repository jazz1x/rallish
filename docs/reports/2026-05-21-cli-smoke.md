# CLI smoke regression — 2026-05-21

Captured against the binary built at HEAD of branch
`feat/cli-polish-and-config-wizard`. `NO_COLOR=1` is set so the
transcripts are easy to diff; all glyphs/ASCII shown are what the
user sees when stdout is not a TTY.

### `rallish --version`

```text
rallish 0.2.1 (commit: 248de435, built: 2026-05-20T16:31:08Z)
```

### `rallish (no args)`

```text

rallish
│  agent-driven rally / squash playbook  ·  0.2.1

◇  setup:    rallish bootstrap
◇  install:  rallish add (interactive)
◇  config:   rallish config list
◇  verify:   rallish doctor

│  for a full command tree, run: rallish --help
```

### `rallish --help`

```text
rallish — agent-driven rally / squash playbook.

A tiny coordinator that lets two coding-CLI agents take turns on the
same problem (rally) or runs one preset headlessly (squash). Skills,
adapters, and presets are bundled and installable via 'rallish add'.

Quick start:
  rallish bootstrap                # one-shot setup wizard
  rallish add                      # interactive component picker
  rallish config list              # see your settings
  rallish doctor                   # check daemon + adapters

For natural-language usage inside a coding-CLI, install the skill
('rallish bootstrap' already does this) and say "랠리보낼 준비해" or
"let's serve" in any project.

Usage:
  rallish [flags]
  rallish [command]

Setup
  bootstrap   Set up rallish in one step (skill + config + daemon check)
  skill       Manage the bundled rallish skill

Rally
  rally       Live baton-passing between interactive CLI sessions
  squash      Run a headless squash session (solo-ralph, pair-review, …)

Manage
  add         Install adapters, presets, or skills
  config      Inspect and edit rallish settings

System
  daemon      Run the rallish broker daemon
  doctor      Diagnose adapters, daemon, and config
  version     Print build version, commit, and date

Additional Commands:
  completion  Generate the autocompletion script for the specified shell
  help        Help about any command

Flags:
  -h, --help      help for rallish
  -v, --version   version for rallish

Use "rallish [command] --help" for more information about a command.
```

### `rallish bootstrap --yes`

```text

rallish setup
│  4 quick steps · enter to accept defaults · ctrl-c to abort
◇  [1/4] installing skill → /tmp/rallish-smoke/home/.claude/skills/rallish
│  created SKILL.ko.md
│  created SKILL.md
│  created install-binary.sh
✓  [1/4] skill installed (3 files)
✓  [2/4] config saved
◇  [3/4] config summary
  ✔ default_preset     → solo-ralph  (/tmp/rallish-smoke/home/.rallish/config.yaml)
  ✔ coding_cli         → claude  (/tmp/rallish-smoke/home/.rallish/config.yaml)
  ✔ wait_mode          → yield  (/tmp/rallish-smoke/home/.rallish/config.yaml)
✓  [3/4] config OK
◇  [4/4] daemon check
✓  [4/4] daemon not running — will auto-spawn on `rally new`

└  setup complete — say "랠리보낼 준비해" (or "let's serve") to start

│  change settings later: rallish config list / set / edit
```

### `rallish config list`

```text
◇  config file: /tmp/rallish-smoke/home/.rallish/config.yaml

KEY              VALUE       SOURCE   DESCRIPTION                                                              
coding_cli       claude      file     Coding-CLI vendor for skill install path (claude / kimi / codex)         
default_adapter  claude      file     Adapter (runtime) presets default to (e.g. claude, kimi)                 
default_preset   solo-ralph  file     Preset used when `rallish squash` is run without --preset                
editor           vi          default  Editor opened by `rallish config edit` (else $EDITOR, $VISUAL, vi)       
telemetry        off         file     Reserved for future opt-in usage telemetry (on / off)                    
wait_mode        yield       file     Default rally WAIT_MODE (yield = poll on user msg; block = blocking join)

│  change a value:  rallish config set <key> <value>
│  open in editor:  rallish config edit
```

### `rallish config get default_preset`

```text
solo-ralph
```

### `rallish config set wait_mode block`

```text
✓  set wait_mode = block
│  file: /tmp/rallish-smoke/home/.rallish/config.yaml
```

### `rallish config path`

```text
/tmp/rallish-smoke/home/.rallish/config.yaml
```

### `rallish doctor`

```text

rallish doctor
◇  daemon             — not running — will auto-spawn on `rally new`
✓  adapter:claude     — /Users/zeppel1n/.local/bin/claude
✓  adapter:kimi       — /Users/zeppel1n/.local/bin/kimi

│  config:  /tmp/rallish-smoke/home/.rallish/config.yaml
│  skill:   /tmp/rallish-smoke/home/.claude/skills/rallish
```

### `rallish add --list`

```text
TYPE     NAME         DESCRIPTION                                                               
adapter  claude       Anthropic Claude CLI adapter                                              
adapter  fake         Deterministic test adapter for rallish                                    
adapter  kimi         Kimi CLI adapter                                                          
preset   pair-review  Planner, executor, and reviewer roles cycling in a structured review loop.
preset   solo-ralph   Single runtime running the ralph loop with budget and exit guards.        
```
